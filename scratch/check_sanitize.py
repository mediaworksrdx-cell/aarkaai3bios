import sys
sys.path.insert(0, '/workspace/aarkaai3b')
import pipeline
res = pipeline._sanitize_query("Write a Python Binary Search Tree class with insert, search, and inorder traversal. Add time complexity comments for each method.")
print("Sanitized query:", res)
EOF
