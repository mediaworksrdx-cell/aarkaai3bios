# ─── AARKAAI Backend – Environment Variables ─────────────────────────────────
# Copy this file to .env and fill in values for your environment.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   source .env  (or use python-dotenv)

# ─── Environment ─────────────────────────────────────────────────────────────
AARKAAI_ENV=production                    # "development" | "production"

# ─── Security ────────────────────────────────────────────────────────────────
AARKAAI_API_KEY=your-secret-api-key-here  # Required in production
AARKAAI_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# ─── Rate Limiting ───────────────────────────────────────────────────────────
AARKAAI_RATE_LIMIT_RPM=30                 # Max requests per minute per IP
AARKAAI_RATE_LIMIT_ENABLED=true           # Explicit enable (auto-enabled in prod)

# ─── Input ───────────────────────────────────────────────────────────────────
AARKAAI_MAX_QUERY_LENGTH=2000             # Max characters per query

# ─── Server ──────────────────────────────────────────────────────────────────
AARKAAI_BASE_URL=https://api.yourdomain.com
AARKAAI_HOST=0.0.0.0
AARKAAI_PORT=5000
AARKAAI_WORKERS=1                          # Keep at 1 for llama.cpp (shared model)

# ─── Database ────────────────────────────────────────────────────────────────
# ─── Database ────────────────────────────────────────────────────────────────
# SQLite (default development)
AARKAAI_DB_URL=sqlite:///aarkaai.db
# MongoDB Atlas / Flex Cluster (recommended for clustered multi-agent memory & persistence)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=aarkaai

# ─── Tool Sandboxing ────────────────────────────────────────────────────────
AARKAAI_SAFE_DIR=/home/ubuntu/aarkaai3b/workspace
AARKAAI_BASH_TIMEOUT=30                   # Max seconds per shell command

# ─── Logging ─────────────────────────────────────────────────────────────────
AARKAAI_LOG_LEVEL=INFO                    # DEBUG | INFO | WARNING | ERROR

# ─── Vertex AI (Google Cloud Platform) ────────────────────────────────────────
# Path to GCP Service Account JSON key (Must have "Vertex AI User" role)
GOOGLE_APPLICATION_CREDENTIALS=/home/ubuntu/aarkaai3b/orbital-heaven-504004-s2-df5a0ce91659.json
VERTEX_PROJECT=orbital-heaven-504004-s2
VERTEX_LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash

# ─── Google Custom Search Engine (Google Search) ─────────────────────────────
# Required for live Google web search retrieval (falls back to DuckDuckGo if unset)
GOOGLE_CSE_API_KEY=your_google_cloud_custom_search_api_key
GOOGLE_CSE_ID=your_programmable_search_engine_cx_id

# ─── Authentication & OAuth ──────────────────────────────────────────────────
AARKAAI_SECRET_KEY=your-random-secret-key-here  # REQUIRED in production (min 32 chars)

# GitHub OAuth (register at https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (register at https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
