package com.aarkaai.app.ui.theme

import androidx.compose.ui.graphics.Color

// ──────── Claude-Inspired Color System ────────

// Backgrounds
val BgLight = Color(0xFFF9F5F1)       // Warm off-white
val BgDark = Color(0xFF1A1A1A)        // Deep charcoal

// Surfaces (cards, input areas)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF2A2A2A)

// Primary Accent (the signature warm orange)
val PrimaryLight = Color(0xFFDA7756)
val PrimaryDark = Color(0xFFE8956E)

// Text
val TextPrimaryLight = Color(0xFF2C2C2C)
val TextPrimaryDark = Color(0xFFE8E8E8)
val TextSecondaryLight = Color(0xFF7A7A7A)
val TextSecondaryDark = Color(0xFF999999)

// User chat bubble
val UserBubbleLight = Color(0xFFF0ECE8)
val UserBubbleDark = Color(0xFF333333)

// AI response area (no bubble, blends with bg)
val AiResponseLight = Color.Transparent
val AiResponseDark = Color.Transparent

// Input pill
val InputBgLight = Color(0xFFFFFFFF)
val InputBgDark = Color(0xFF2C2C2C)
val InputBorderLight = Color(0xFFE0DCD8)
val InputBorderDark = Color(0xFF444444)

// Sidebar
val SidebarBgLight = Color(0xFFF2EEEA)
val SidebarBgDark = Color(0xFF222222)
val SidebarItemHoverLight = Color(0xFFE8E4E0)
val SidebarItemHoverDark = Color(0xFF333333)

// Code blocks
val CodeBgLight = Color(0xFFF5F2EF)
val CodeBgDark = Color(0xFF1E1E1E)

// Dividers
val DividerLight = Color(0xFFEAE6E2)
val DividerDark = Color(0xFF3A3A3A)

// Status
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFE53935)
val WarningAmber = Color(0xFFFFA726)
